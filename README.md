# Tri-Parish Image Bundle v2

Unzip this into the root of the Hugo project:

cd ~/Documents/TriParishOK
unzip -o ~/Downloads/tri-parish-image-bundle-v2.zip -d .

This adds image files, image metadata, and image credit documentation.
